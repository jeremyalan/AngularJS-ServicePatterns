section
    h2 Service

    div
        span module.service(...)

    pre.
        function PersonService($http) {
            this.create = (info) {
                return $http.post('/api/person', info);
            };
        }

        angular.module('api')
            .service('$person', <span class="highlight">PersonService</span>)
            .service('$person', <span class="highlight">['$http', PersonService]</span>)